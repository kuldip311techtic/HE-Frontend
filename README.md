# HE-Frontend

Super Admin panel for Hoops Engine.

## Scripts

```bash
npm install
npm run dev
npm run typecheck
npm run lint
npm test
npm run build
npm run format
```

The app reads the API base URL from `VITE_API_BASE_URL` (see `.env.example`). Super Admins sign in at `/admin/login`.
