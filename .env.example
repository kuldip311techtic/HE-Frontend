# Relative /api path uses the Vite dev/preview proxy to avoid CORS during local development.
VITE_API_BASE_URL=/api

# Seeds a super-admin validation session so Luna live validation can reach gated admin routes.
# Set to true for Luna validation captures; omit or false for normal local development.
VITE_LUNA_VALIDATION=true
